{
  "name": "nokotuber",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Aplicativo de PNGTuber para desktop: avatar em PNG que reage ao microfone.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "pnpm csp:config && pnpm tauri dev --config src-tauri/tauri.conf.csp.json",
    "tauri:build": "pnpm csp:config && pnpm tauri build --config src-tauri/tauri.conf.csp.json",
    "csp:config": "node --env-file-if-exists=.env scripts/tauri-config.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.7.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "@tsconfig/svelte": "^5.0.0",
    "@types/node": "^24.0.0",
    "svelte": "^4.0.0",
    "svelte-check": "^3.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
